spring:
  application:
    name: student_details

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:jdbc:mysql://localhost:3306/login}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:sowmya}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    generate-ddl: true

  main:
    allow-circular-references: true

server:
  port: ${PORT:8080}